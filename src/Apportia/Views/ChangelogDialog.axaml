<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:text="using:Apportia.Text"
        x:Class="Apportia.Views.ChangelogDialog"
        Title="{x:Static text:UiText+Dialog.ChangelogTitle}"
        Width="640"
        Height="560"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        ShowInTaskbar="False"
        Background="{DynamicResource AppWindowBrush}">

    <Grid RowDefinitions="Auto,Auto,*,Auto">

        <StackPanel Grid.Row="0" Margin="20,18,20,14" Spacing="3">
            <TextBlock x:Name="VersionText"
                       FontSize="16" FontWeight="SemiBold"
                       Foreground="{DynamicResource AppTextBrush}" />
            <TextBlock Text="{x:Static text:UiText+Dialog.ChangelogSubtitle}"
                       FontSize="13"
                       Foreground="{DynamicResource AppSubTextBrush}" />
        </StackPanel>

        <Border Grid.Row="1" Height="1" Background="{DynamicResource AppSeparatorBrush}" />

        <ScrollViewer Grid.Row="2"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      Padding="20,10,0,10">
            <StackPanel Margin="0,0,48,0">
                <StackPanel x:Name="ChangelogPanel" />
                <Border Height="40" />
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="3" BorderBrush="{DynamicResource AppSeparatorBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="20,12" Spacing="8">
                <Button Content="{x:Static text:UiText+Button.InstallUpdate}" Click="OnInstall" />
                <Button Content="{x:Static text:UiText+Button.Cancel}" Click="OnCancel" />
            </StackPanel>
        </Border>

    </Grid>
</Window>